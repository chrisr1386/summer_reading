<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Summer Reading", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'finished_books' %>">
          <%= link_to "Books That Have Been Read", finished_books_path %>
        </li>

        <li class="<%= 'active' if params[:controller] == 'book_recommendations' && controller.action_name != 'by_teacher' && !params[:by_teacher] %>">
          <%= link_to "Book Recommendations", book_recommendations_path %></li>
        </li>

        <li class="<%= 'active' if params[:controller] == 'book_recommendations' && params[:by_teacher] %>">
          <%= link_to "Recommendations From Mrs. Root", root_recommendations_path + "?by_teacher=true" %>
        </li>
      </ul>

      <% if current_user %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" href="#">
              <%= current_user.email %>
              <span class="caret"></span>
            </a>

            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Sign out", sign_out_path, method: :delete %></li>
            </ul>
          </li>
        </ul>
      <% end %>

    </div><!--/.nav-collapse -->
  </div>
</nav>